/*
package controller;

import java.util.UUID;

public class bookingController {

    public static String confirmBooking(String name, String gender, String phone, String ic, String paymentMethod, double totalPrice) {
        String bookingID = "BKG" + UUID.randomUUID().toString().substring(0, 6).toUpperCase();
        return bookingID;
    }
}
*/
